<div class="container-fluid px-4">
  <h1 class="mt-4">Dashboard</h1>
  

  <div class="row">
    <div class="col-12">
      <div class="card mb-4">
        <div class="card-body">
          <h2 class="mb-3">Welcome to My Project</h2>
          <% if user_signed_in? %>
            <p class="text-success">You are logged in as <strong><%= current_user.email %></strong>.</p>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

